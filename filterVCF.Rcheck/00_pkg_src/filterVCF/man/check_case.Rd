% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter_VCF_utilities.R
\name{check_case}
\alias{check_case}
\title{A function to check if element of a vector are upper of lower case}
\usage{
check_case(string, Sample = NULL, isupper = TRUE)
}
\arguments{
\item{string}{A vector of string to check}

\item{Sample}{should the string vector be subsample to do a quick test assuming that the case (upper or lower) is the same for all element of the vector. Default is NULL, all element are checked.}

\item{isupper}{Test if string is upper is TRUE, lower if FALSE}
}
\value{
A logical vector of the size of string or size of subsample
}
\description{
A function to check if elemeent are upper of lower case. If the string
vector is to long a quick check can be used on a random sample of elements to
check there case. Note that mixed case will always return FALSE (e.g. "Ab"),
}
\examples{

check_case(LETTERS[1:10])
check_case(c(LETTERS[1:10], letters[1:10]))
check_case(c(LETTERS[1:10], letters[1:10]), isupper = FALSE)
check_case(c(LETTERS[1:10], letters[1:10]), Sample = 5, isupper = FALSE)
}
\references{

}
\author{
~~Benjamin Laenen~~
 objects to See Also as \code{\link{help}},
}
\keyword{~utilities}
